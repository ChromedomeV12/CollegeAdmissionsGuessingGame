# Copy this file to deploy/private.env. That file is ignored by Git.
# Generate at least 32 random bytes and paste the result after JWT_SECRET=.
JWT_SECRET=

# The service remains bound to VM loopback. Change only if you understand the
# exposure implications and have added a reviewed access proxy and rate limits.
APP_PORT=3005
